package service;

import model.cart.CartItem;

import model.order.OrderObserver;

import java.util.List;
//todo
/*
public class OrderService {
    public Order createOrder(List<CartItem> cartItems) {
        return new Order(cartItems);
    }

    public void addOrderObserver(Order order, OrderObserver observer) {
        order.addObserver(observer);
    }

    public void placeOrder(Order order, String orderStatus) {
        order.setOrderStatus(orderStatus);
    }
}
*/